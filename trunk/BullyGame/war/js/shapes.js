function Square(x, y, size, color)
{
	this.x = x;
	this.y = y;
	this.size = size;
	this.color = color;
	this.isFilled = false;
}

function Pawn(x, y, size, color)
{
	this.x = x;
	this.y = y;
	this.size = size;
	this.color = color;
}